namespace DionysosFX.Host
{
    /// <summary>
    /// Web server changable states
    /// </summary>
    public enum WebServerState
    {
        Created,
        Loading,
        Listening,
        Stopped
    }
}
